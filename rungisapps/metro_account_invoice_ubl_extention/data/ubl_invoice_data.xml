<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- UBL Button in action menu -->
        <record id="model_account_invoice_action_ubl" model="ir.actions.server">
            <field name="name">Generate UBL XML File</field>
            <field name="model_id" ref="account.model_account_invoice"/>
            <field name="binding_model_id" ref="account.model_account_invoice"/>
            <field name="state">code</field>
            <field name="code">action = record.generates_ubl_xml_file_button()</field>
        </record>
    </data> 
</odoo>